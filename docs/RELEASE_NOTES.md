# Release Notes

## v3.3 — June 8, 2026

- Patient info editing in encounter modal with audit trail until visit is completed.
- Encounter modal SOAP editing: AI notes read-only, doctor notes editable until visit is completed.
- Doctor SOAP PDF download and email-to-patient in encounter modal.
- SOAP edit audit trail (who changed notes last).
- Server-side login for clinics that block direct Supabase access.
- Admin pharmacy and middleware access fixes.
- Resend pharmacy email and admin navigation updates.
- Doctor NPI on profile and prescription emails (replaces EIN).
- Nurse walk-in encounter improvements.
- Inactive user cleanup (operations).

---

## Earlier releases

Version history before v3.3 was not tracked in this file. Application version is mirrored in `package.json` and exposed via `/api/health`.
