# dependencies
node_modules/

# next.js
.next/
out/
playwright-report/
test-results/
blob-report/

# typescript build cache
*.tsbuildinfo

# env files
.env
.env.local
# local secrets file (holds SUPABASE_SECRET_KEY) — bare name, not matched by `.env*`
env

# misc
.DS_Store
.cache/

# audit report (local only)
report.md

# Claude Code local session settings (MCP permissions, etc.)
.claude/settings.local.json
